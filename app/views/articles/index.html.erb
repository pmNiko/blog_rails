<h1> Articles</h1>

<% @articles.each do |article| %>
  <h3>
    <%= link_to article.title, article_path(article) %>
  </h3>
<%end%>
<% if current_user.present?%>
  <%= link_to 'New article', new_article_path %>
<%end%>

<%= link_to 'Back to welcome', welcome_article_path %>
